## 意图

本次变更旨在消除中文文档中对 nonce 定义的歧义，明确 `Biz-Api-Nonce` 不是任意随机字符串，而是当前 Unix 时间戳（毫秒）。重点修正文档中“随机数”“随机字符串”与“毫秒级 Unix 时间戳”混用的问题，避免读者误解请求头和签名串中的 nonce 取值规则。与此同时，也同步整理英文对应页面和应用认证页中的相关描述，使跨页面、跨语言的 nonce 定义保持一致。

## 变更摘要

- `v2_cn/guides/overview/cobo-auth.mdx`：将中文认证总览页中的 `Biz-Api-Nonce`、`## Nonce` 小节和签名表格统一改为“当前 Unix 时间戳（毫秒）”，并显式绑定到请求头 `Biz-Api-Nonce`，消除“随机数”歧义。
- `v2_cn/guides/get-started/get-started-with-waas.mdx`：更新中文入门页中请求头说明，明确 `Biz-Api-Nonce` 的实际取值定义，防止歧义沿入门链路继续传播。
- `v2_cn/apps/authentication.mdx`：调整中文应用认证页中的注释和签名表格文案，统一 `TIMESTAMP` 与 `Biz-Api-Nonce` 的对应关系。
- `v2/guides/overview/cobo-auth.mdx`：优化英文认证总览页对 nonce 的描述，明确 Nonce 指 `Biz-Api-Nonce` 请求头的值，并要求与签名内容完全一致。
- `v2/guides/get-started/get-started-with-waas.mdx`：更新英文入门页中 `Biz-Api-Nonce` 的说明，使其与认证总览页表述一致。
- `v2/apps/authentication.mdx`：同步调整英文应用认证页中的 `TIMESTAMP` 定义，明确它必须等于 `Biz-Api-Nonce` 请求头的值。

## 待确认事项

- 需要人工核实服务端 API 认证或签名校验实现中对 `Biz-Api-Nonce` 的解析规则，确认是否强制要求毫秒级 Unix 时间戳、是否仅要求唯一性，以及是否存在时间窗口或过期校验。
- 需要人工核实官方 SDK 或签名示例代码中 nonce 的生成实现，确认客户端是否始终以当前 Unix 时间戳（毫秒）填充 `Biz-Api-Nonce`。
- 本次修改依据现有中英文文档对齐和页面内一致性清理完成，未在本仓库内检索到认证网关、API handler 或 SDK 源码证据，因此在将该定义视为最终权威前仍需结合实现再次确认。
