type: object
description: The event data type.
required:
  - data_type
properties:
  data_type:
    type: string
    enum:
      - Transaction
    description: The model of data.
    example: 'Transaction'