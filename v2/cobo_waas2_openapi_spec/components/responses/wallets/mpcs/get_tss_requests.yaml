description: Successfully retrieved TSS request.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '../../../schemas/wallets/mpcs/tss_request.yaml'
        pagination:
          $ref: '../../../schemas/pagination/metadata.yaml'
