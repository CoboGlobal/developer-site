description: Successfully listed all projects.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '../../../schemas/wallets/mpcs/project.yaml'
        pagination:
          $ref: '../../../schemas/pagination/metadata.yaml'
