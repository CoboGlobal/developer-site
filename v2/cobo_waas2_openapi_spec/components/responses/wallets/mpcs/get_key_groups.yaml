description: Successfully listed key share holder groups.
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: '../../../schemas/wallets/mpcs/key_group.yaml'
        pagination:
          $ref: '../../../schemas/pagination/metadata.yaml'

